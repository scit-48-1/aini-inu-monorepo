---
phase: 07-walk-diary-story
plan: 03
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified:
  - aini-inu-frontend/src/components/profile/MyProfileView.tsx
autonomous: true
gap_closure: true
requirements:
  - DIARY-01
  - DIARY-02
  - DIARY-03
  - DIARY-04
  - DIARY-05
  - DIARY-06
  - DIARY-07

must_haves:
  truths:
    - "User can delete a diary from the DiaryBookModal with a confirmation dialog"
    - "Trash icon renders in DiaryPageRenderer when viewing own diary in profile mode"
  artifacts:
    - path: "aini-inu-frontend/src/components/profile/MyProfileView.tsx"
      provides: "onDelete prop wired to DiaryBookModal"
      contains: "onDelete="
  key_links:
    - from: "MyProfileView.tsx"
      to: "DiaryBookModal"
      via: "onDelete prop"
      pattern: "onDelete=.*handleDelete"
---

<objective>
Wire the missing diary delete path and add a confirmation dialog before deletion.

Purpose: DIARY-05 (diary delete) is partially implemented -- handleDelete exists in useWalkDiaries and DiaryBookModal accepts onDelete, but MyProfileView never passes the prop. The Trash2 icon in DiaryPageRenderer never renders because onDelete is undefined. This is a one-file wiring fix.

Output: Working diary delete flow with confirmation dialog accessible from the profile DiaryBookModal.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-walk-diary-story/07-VERIFICATION.md

@aini-inu-frontend/src/components/profile/MyProfileView.tsx
@aini-inu-frontend/src/hooks/useWalkDiaries.ts
@aini-inu-frontend/src/components/profile/DiaryBookModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire onDelete prop and add confirmation dialog</name>
  <files>aini-inu-frontend/src/components/profile/MyProfileView.tsx</files>
  <action>
In MyProfileView.tsx, find the DiaryBookModal JSX (around line 406-413) and add the onDelete prop. The prop should wrap handleDelete (already destructured from useWalkDiaries at line 187) with a window.confirm confirmation dialog before executing.

Specifically, change the DiaryBookModal rendering from:
```jsx
<DiaryBookModal
  isOpen={!!selectedHistory}
  onClose={() => setSelectedHistory(null)}
  mode="profile"
  selectedDiaryId={selectedHistory?.id}
  diaries={diaries}
  onSaveSuccess={fetchData}
/>
```

To:
```jsx
<DiaryBookModal
  isOpen={!!selectedHistory}
  onClose={() => setSelectedHistory(null)}
  mode="profile"
  selectedDiaryId={selectedHistory?.id}
  diaries={diaries}
  onSaveSuccess={fetchData}
  onDelete={async (diaryId: number) => {
    if (!window.confirm('산책일기를 삭제하시겠습니까?')) return;
    await handleDelete(diaryId);
    setSelectedHistory(null);
  }}
/>
```

Key details:
- window.confirm with Korean text for the confirmation step (matches project pattern of simple confirm dialogs)
- After successful delete, close the modal by setting selectedHistory to null
- handleDelete already handles the API call, success toast, and diary list refetch
- No other files need changes -- DiaryBookModal and DiaryPageRenderer already support onDelete prop correctly
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx next lint && npm run build</automated>
  </verify>
  <done>Trash2 icon visible in DiaryPageRenderer when viewing own diary in profile mode. Clicking it shows Korean confirmation dialog. Confirming deletes the diary, shows success toast, refreshes diary list, and closes the modal.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no type errors
2. `npm run lint` passes with no warnings
3. Grep confirms onDelete is passed to DiaryBookModal in MyProfileView.tsx
</verification>

<success_criteria>
- DiaryBookModal in MyProfileView.tsx receives onDelete prop
- Confirmation dialog appears before delete execution
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-walk-diary-story/07-03-SUMMARY.md`
</output>
