---
phase: 06-walk-threads
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - aini-inu-frontend/src/components/around-me/RadarSidebar.tsx
  - aini-inu-frontend/src/components/around-me/RadarMapSection.tsx
autonomous: true
gap_closure: true
requirements: [WALK-03]

must_haves:
  truths:
    - "Full threads (currentParticipants >= maxParticipants) show '모집 완료' badge instead of time remaining"
    - "Apply button is disabled when thread is full"
    - "Full threads are still visible in sidebar and map (not hidden)"
  artifacts:
    - path: "aini-inu-frontend/src/components/around-me/RadarSidebar.tsx"
      provides: "'모집 완료' badge on full thread cards"
      contains: "모집 완료"
    - path: "aini-inu-frontend/src/components/around-me/RadarMapSection.tsx"
      provides: "'모집 완료' badge in popup + disabled apply button"
      contains: "모집 완료"
  key_links:
    - from: "RadarSidebar.tsx thread card"
      to: "currentParticipants >= maxParticipants"
      via: "isFull boolean check"
      pattern: "currentParticipants.*>=.*maxParticipants"
    - from: "RadarMapSection.tsx apply button"
      to: "disabled prop"
      via: "isFull check"
      pattern: "isFull"
---

<objective>
Add '모집 완료' (recruitment closed) badge for full threads in sidebar and map popup.

Purpose: UAT found that threads at capacity are displayed identically to open threads. Users need visual distinction between "expired" (hidden) and "full" (visible with badge). Full threads should show a badge and disable the apply button.
Output: RadarSidebar and RadarMapSection show '모집 완료' badge when currentParticipants >= maxParticipants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-walk-threads/06-UAT-retest.md
</context>

<interfaces>
From threads.ts:
```typescript
export interface ThreadSummaryResponse {
  currentParticipants: number;
  maxParticipants: number;
  // ...other fields
}

export interface ThreadResponse {
  currentParticipants: number;
  maxParticipants: number;
  // ...other fields
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add '모집 완료' badge to RadarSidebar and RadarMapSection</name>
  <files>
    aini-inu-frontend/src/components/around-me/RadarSidebar.tsx,
    aini-inu-frontend/src/components/around-me/RadarMapSection.tsx
  </files>
  <action>
    **RadarSidebar.tsx:**
    In the `sortedThreads.map()` render block (around line 128), after computing `isParticipating` and `expired2`:
    1. Add: `const isFull = thread.currentParticipants >= thread.maxParticipants;`
    2. In the badges area (lines 159-174), add a third condition. The badge priority order should be:
       - If expired/expired2: show "만료됨" badge (red) -- existing
       - Else if isFull: show "모집 완료" badge (blue): `<Badge variant="default" className="bg-blue-100 text-blue-600 border-none px-3 py-1 text-[10px]">모집 완료</Badge>`
       - Else: show remaining time badge (amber) -- existing
    3. Keep the "참여 중" badge as-is (it shows independently below).

    **RadarMapSection.tsx:**
    In the thread detail popup section (around line 274):
    1. After computing `isApplied` and `isOwner`, add: `const isFull = selectedThread ? selectedThread.currentParticipants >= selectedThread.maxParticipants : false;`
    2. In the badges row (around line 281-288), after the chatType badge:
       - Add: if isFull and not expired, show `<Badge variant="default" className="bg-blue-100 text-blue-600 border-none px-3 text-[10px]">모집 완료</Badge>`
    3. In the action buttons section (around line 461-470), the "산책 신청하기" button (the else branch for non-owner, non-applied):
       - Add `disabled={isFull}` to the Button
       - Change label conditionally: `{isFull ? '정원이 찼습니다' : '산책 신청하기'}`
       - Add disabled styling: when isFull, add `opacity-50 cursor-not-allowed` classes
       - Also prevent `onClick` from firing when isFull: wrap in `onClick={() => !isFull && setShowPetSelect(true)}`
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    Full threads display "모집 완료" blue badge in both sidebar cards and map popup. Apply button is disabled with "정원이 찼습니다" text when thread is full. Expired threads still show "만료됨" red badge. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. Frontend compiles: `npx tsc --noEmit` exits 0
2. Manual verification: View a full thread (currentParticipants == maxParticipants) in sidebar -- blue "모집 완료" badge visible
3. Manual verification: Click full thread on map -- popup shows "모집 완료" badge and apply button is disabled with "정원이 찼습니다"
4. Manual verification: Non-full threads still show remaining time badge and enabled apply button
</verification>

<success_criteria>
- Full threads (currentParticipants >= maxParticipants) show "모집 완료" blue badge in sidebar
- Full threads show "모집 완료" badge in map popup
- Apply button disabled when thread is full
- Expired threads still show "만료됨" and remain hidden per existing behavior
- Non-full threads unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-walk-threads/06-08-SUMMARY.md`
</output>
