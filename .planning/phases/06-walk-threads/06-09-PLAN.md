---
phase: 06-walk-threads
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - aini-inu-frontend/src/app/around-me/page.tsx
  - aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx
  - aini-inu-frontend/src/components/common/DynamicMap.tsx
  - aini-inu-frontend/src/components/around-me/RadarMapSection.tsx
autonomous: true
gap_closure: true
requirements: [WALK-02]

must_haves:
  truths:
    - "Edit mode bypasses active thread guard"
    - "Refresh button appears after all filter controls"
    - "Map circle radius matches selected radius value"
    - "GPS location shows '현재 위치' in location button"
  artifacts:
    - path: "aini-inu-frontend/src/app/around-me/page.tsx"
      provides: "editingThreadId bypasses myActiveThread guard"
      contains: "editingThreadId"
    - path: "aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx"
      provides: "Refresh button moved after filters"
    - path: "aini-inu-frontend/src/components/common/DynamicMap.tsx"
      provides: "Dynamic circle radius from prop"
      contains: "radiusKm"
  key_links:
    - from: "page.tsx RECRUIT tab"
      to: "editingThreadId check"
      via: "conditional rendering"
      pattern: "editingThreadId"
    - from: "RadarMapSection"
      to: "DynamicMap radiusKm"
      via: "prop passing"
      pattern: "radiusKm"
---

<objective>
Fix 4 quick UI issues: edit mode bypassing active thread guard, header button order, dynamic circle radius, and GPS location label.

Purpose: UAT found that (1) clicking edit is blocked by active thread guard, (2) refresh button should be last, (3) map circle is hardcoded 2500m, (4) GPS location shows stale address instead of "현재 위치".
Output: All 4 issues resolved as frontend-only changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-walk-threads/06-UAT-retest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix edit mode guard bypass + header button order + circle radius + GPS label</name>
  <files>
    aini-inu-frontend/src/app/around-me/page.tsx,
    aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx,
    aini-inu-frontend/src/components/common/DynamicMap.tsx,
    aini-inu-frontend/src/components/around-me/RadarMapSection.tsx
  </files>
  <action>
    **1. page.tsx — Edit mode bypasses active thread guard (BLOCKER):**
    Line 118, change the condition from:
    ```tsx
    myActiveThread ? (
    ```
    to:
    ```tsx
    (myActiveThread && !editingThreadId) ? (
    ```
    This lets the edit form render when editingThreadId is set, even if the user has an active thread.

    **2. AroundMeHeader.tsx — Move refresh button after date + radius:**
    In the `<div className="flex items-center gap-2">` block (lines 49-96):
    - Move the `{onRefresh && (...)}` block (lines 53-63) to AFTER the `{onRadiusChange && (...)}` block (after line 95).
    - Result order: 동네설정 → 날짜 → radius → 재탐색

    **3. DynamicMap.tsx — Dynamic circle radius:**
    a. Add `radiusKm?: number` to `DynamicMapProps` interface (line 9-16).
    b. Add `radiusKm` to the function params (line 47), default to 2.5.
    c. Change Circle radius from `2500` (line 104) to `(radiusKm ?? 2.5) * 1000`.
    d. Update the Circle key to include radiusKm: `key={`circle-${center[0]}-${center[1]}-${radiusKm}`}`

    **4. RadarMapSection.tsx — Pass radius to DynamicMap:**
    a. Add `radius?: number` to `RadarMapSectionProps` interface (line 73-86).
    b. Add `radius` to destructured props (line 88-101).
    c. Pass to DynamicMap (line 233-238): `<DynamicMap ... radiusKm={radius} />`

    **5. page.tsx — Pass radius to RadarMapSection:**
    In the `<RadarMapSection` JSX (line 85-98), add `radius={radius}`.

    **6. useRadarLogic.ts — GPS label reset:**
    In the GPS `useEffect` (lines 107-131), after both `applyCoords` calls, add:
    - After the success callback (line 119, inside the getCurrentPosition success): the location is GPS-based, so no change needed (we want to keep whatever the user set).
    - Actually, the issue is that on page load, searchCoordinates is null (GPS mode), but currentLocation from the store still shows the old address.
    - In the `applyCoords` function (line 108-112), add `setLocation('현재 위치')` import from useConfigStore, call it inside applyCoords so on every fresh GPS acquisition the label resets.

    Wait — `setLocation` comes from `useConfigStore` which is used in page.tsx. Let me adjust:
    - In useRadarLogic.ts, the hook already has `const { setCoordinates } = useConfigStore();`.
    - Change to: `const { setCoordinates, setLocation } = useConfigStore();`
    - In the `applyCoords` function, add `setLocation('현재 위치');` after `setCoordinates(coords)`.
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    (1) Edit mode bypasses active thread guard. (2) Refresh button appears after filters. (3) Map circle scales with radius. (4) GPS location shows "현재 위치". TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
1. Frontend compiles: `npx tsc --noEmit` exits 0
2. Manual: Click "수정하기" on own thread → RECRUIT tab shows edit form, not "이미 활성 모집글" banner
3. Manual: Header order is 동네설정 → 날짜 → radius → 재탐색
4. Manual: Change radius to 10km → map circle grows proportionally
5. Manual: Refresh page → location button shows "현재 위치"
</verification>

<success_criteria>
- Edit button leads to edit form, not blocked by active thread guard
- Refresh button is last in the header filter row
- Map circle radius changes when radius dropdown value changes
- Fresh page load shows "현재 위치" in location button
</success_criteria>

<output>
After completion, create `.planning/phases/06-walk-threads/06-09-SUMMARY.md`
</output>
