---
phase: 04-member-profile-relations
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - aini-inu-frontend/src/components/profile/MyProfileView.tsx
autonomous: true
gap_closure: true
requirements:
  - MEM-01

must_haves:
  truths:
    - "Own-profile DOGS tab fetches pets from GET /pets (getMyPets) instead of GET /members/0/pets"
  artifacts:
    - path: "aini-inu-frontend/src/components/profile/MyProfileView.tsx"
      provides: "Own profile view with correct pet fetching"
      contains: "getMyPets"
  key_links:
    - from: "src/components/profile/MyProfileView.tsx"
      to: "/api/v1/pets"
      via: "getMyPets() from @/api/pets"
      pattern: "getMyPets\\(\\)"
---

<objective>
Fix own-profile pet list to use getMyPets() instead of placeholder getMemberPets(0).

Purpose: The DOGS tab on the own-profile page calls getMemberPets(0), which hits /members/0/pets — a non-existent member. The correct function getMyPets() (hitting GET /pets) already exists in api/pets.ts but is not used. This closes the single verification gap blocking MEM-01.
Output: MyProfileView.tsx with correct pet API call.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-member-profile-relations/04-VERIFICATION.md

<interfaces>
<!-- From aini-inu-frontend/src/api/pets.ts -->
```typescript
export async function getMyPets(): Promise<PetResponse[]>;
```

<!-- From aini-inu-frontend/src/api/members.ts (currently imported, getMemberPets to be removed) -->
```typescript
export async function getMemberPets(memberId: number): Promise<PetResponse[]>;
```

<!-- Current import in MyProfileView.tsx line 8: -->
```typescript
import { getMe, getWalkStats, getFollowers, getFollowing, getMemberPets } from '@/api/members';
```
<!-- Line 10 already imports PetResponse type from @/api/pets -->
```typescript
import type { PetResponse } from '@/api/pets';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace getMemberPets(0) with getMyPets() in MyProfileView</name>
  <files>aini-inu-frontend/src/components/profile/MyProfileView.tsx</files>
  <action>
    1. Update import on line 8: remove `getMemberPets` from the `@/api/members` import (keep getMe, getWalkStats, getFollowers, getFollowing).
    2. Add `getMyPets` to the existing `@/api/pets` import on line 10, changing it from a type-only import to a mixed import:
       `import { getMyPets } from '@/api/pets';`
       `import type { PetResponse } from '@/api/pets';`
       Or combine: `import { getMyPets, type PetResponse } from '@/api/pets';`
    3. On line 221 in fetchData, replace `getMemberPets(0)` with `getMyPets()`.
    4. Remove the placeholder comment `// 0 = placeholder, will fetch from /pets via getMyPets in Task 3` on the same line.
    5. Verify no other references to getMemberPets remain in this file (there should be none).
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>MyProfileView.tsx imports getMyPets from @/api/pets, calls getMyPets() in fetchData instead of getMemberPets(0), build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- grep for `getMemberPets` in MyProfileView.tsx returns no results
- grep for `getMyPets` in MyProfileView.tsx returns at least 1 result (the import and the call)
- `npm run build` succeeds without errors
</verification>

<success_criteria>
Own-profile DOGS tab calls GET /pets (via getMyPets) instead of GET /members/0/pets. The placeholder getMemberPets(0) is fully removed from MyProfileView.tsx. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-member-profile-relations/04-04-SUMMARY.md`
</output>
