---
phase: 10-lost-pet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aini-inu-frontend/src/app/around-me/page.tsx
  - aini-inu-frontend/src/components/around-me/EmergencyReportForm.tsx
  - aini-inu-frontend/src/components/around-me/EmergencySubTabs.tsx
  - aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx
autonomous: true
requirements: [LOST-01, LOST-02, LOST-07]

must_haves:
  truths:
    - "EMERGENCY tab no longer shows '준비 중' overlay -- it shows functional sub-tabs"
    - "User can fill in lost pet report form with photo upload, breed text, datetime, DaumPostcode location, and description, then submit"
    - "User can fill in sighting form with photo upload, DaumPostcode location, datetime, and optional memo, then submit"
    - "LOST/FOUND toggle switches between lost pet report form and sighting form"
    - "Image upload uses presigned URL flow (not base64)"
    - "geminiService import is completely removed from EmergencyReportForm"
  artifacts:
    - path: "aini-inu-frontend/src/components/around-me/EmergencySubTabs.tsx"
      provides: "Sub-tab controller within EMERGENCY (신고/제보 작성 | 내 신고 목록)"
      min_lines: 20
    - path: "aini-inu-frontend/src/components/around-me/EmergencyReportForm.tsx"
      provides: "Rewritten lost pet + sighting forms using backend API"
      min_lines: 80
    - path: "aini-inu-frontend/src/app/around-me/page.tsx"
      provides: "EMERGENCY tab wired to sub-tabs without overlay"
  key_links:
    - from: "EmergencyReportForm.tsx"
      to: "api/lostPets.ts"
      via: "createLostPet() and createSighting() calls"
      pattern: "createLostPet|createSighting"
    - from: "EmergencyReportForm.tsx"
      to: "api/upload.ts"
      via: "uploadImageFlow(file, purpose)"
      pattern: "uploadImageFlow"
    - from: "around-me/page.tsx"
      to: "EmergencySubTabs.tsx"
      via: "EMERGENCY tab renders EmergencySubTabs"
      pattern: "EmergencySubTabs"
---

<objective>
Wire the EMERGENCY tab with sub-tabs and rewrite the report/sighting forms to use backend API endpoints.

Purpose: Activate the EMERGENCY tab (currently disabled with overlay) and provide functional lost pet report and sighting creation using the 7 backend API endpoints. This is the foundation for the AI analysis flow in Plan 02/03.
Output: Working EMERGENCY tab with sub-tab navigation, lost pet report form (LOST-01), and sighting quick form (LOST-02).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-lost-pet/10-CONTEXT.md
@.planning/phases/10-lost-pet/10-RESEARCH.md

@aini-inu-frontend/src/api/lostPets.ts
@aini-inu-frontend/src/api/upload.ts
@aini-inu-frontend/src/api/types.ts
@aini-inu-frontend/src/app/around-me/page.tsx
@aini-inu-frontend/src/components/around-me/EmergencyReportForm.tsx
@aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx

<interfaces>
<!-- Key types the executor needs -->

From api/lostPets.ts:
```typescript
export interface LostPetCreateRequest {
  petName: string;
  breed: string;
  photoUrl: string;
  description: string;
  lastSeenAt: string;       // ISO 8601 format
  lastSeenLocation: string;
}
export interface LostPetResponse {
  lostPetId: number;
  status: string;
  createdAt: string;
}
export interface SightingCreateRequest {
  photoUrl: string;
  foundAt: string;          // ISO 8601 format
  foundLocation: string;
  memo?: string;
}
export interface SightingResponse {
  sightingId: number;
  status: string;
  foundAt: string;
}
export async function createLostPet(data: LostPetCreateRequest): Promise<LostPetResponse>;
export async function createSighting(data: SightingCreateRequest): Promise<SightingResponse>;
```

From api/upload.ts:
```typescript
export async function uploadImageFlow(file: File, purpose: string): Promise<string>;
```

From api/types.ts:
```typescript
export class ApiError extends Error {
  constructor(public errorCode: string, message: string, public status: number);
}
export type AsyncState = 'idle' | 'loading' | 'empty' | 'error' | 'success';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmergencySubTabs + rewrite EmergencyReportForm</name>
  <files>
    aini-inu-frontend/src/components/around-me/EmergencySubTabs.tsx,
    aini-inu-frontend/src/components/around-me/EmergencyReportForm.tsx
  </files>
  <action>
**EmergencySubTabs.tsx** -- NEW file:
- Two sub-tabs: "신고/제보 작성" | "내 신고 목록"
- Controlled component: `activeSubTab` state, `onSubTabChange` callback
- Visual style: pill toggle (consistent with LOST/FOUND toggle already in the form). Use same pattern as the existing FIND/RECRUIT/EMERGENCY tab nav but smaller/simpler.
- "내 신고 목록" content is a placeholder div with text "내 신고 목록" for now (Plan 02 builds this out).
- "신고/제보 작성" renders EmergencyReportForm.
- Export the component.

**EmergencyReportForm.tsx** -- FULL REWRITE:
- Remove ALL imports of `geminiService`, `DOG_BREEDS`, `DogBreed`, `analyzeDogImage`. Remove the `optimizeImage` prop entirely.
- New props interface: `{ onReportCreated?: (report: LostPetResponse, photoUrl: string) => void }` -- callback after successful lost pet creation (Plan 03 will chain AI analysis from this).
- Keep the existing LOST/FOUND toggle (mode state). The toggle controls which form is shown.

**LOST mode form (lost pet report):**
- Image upload: file input -> preview shown (use object URL, not base64). Store the File object in state.
- Fields: petName (text input, required), breed (manual text input, no dropdown per locked decision), lastSeenAt (datetime-local input), lastSeenLocation (DaumPostcode button + display), description (textarea).
- On submit: (1) call `uploadImageFlow(file, 'LOST_PET')` to get photoUrl, (2) call `createLostPet({ petName, breed, photoUrl, description, lastSeenAt: isoString, lastSeenLocation })`, (3) toast success, (4) call `onReportCreated?.(response, photoUrl)`, (5) reset form.
- lastSeenAt: convert datetime-local value to ISO 8601 string (`new Date(value).toISOString()` or just send the value directly as `YYYY-MM-DDTHH:mm` which Jackson accepts).
- DaumPostcode: import from `react-daum-postcode`. Show a "주소 검색" button. On address selection, display the address and store in state. Same modal pattern as RecruitForm.

**FOUND mode form (sighting quick form):**
- Photo first UX: large camera/upload area at top (photo is required).
- Fields: foundLocation (DaumPostcode), foundAt (datetime-local), memo (optional textarea).
- On submit: (1) `uploadImageFlow(file, 'SIGHTING')`, (2) `createSighting({ photoUrl, foundAt, foundLocation, memo })`, (3) toast success "제보가 등록되었습니다!", (4) reset form.
- Simpler layout than LOST form per locked decision -- optimized for speed.

**Both forms:**
- Show loading state during upload + API call (disable submit button, show Loader2 spinner).
- Use existing Card, Typography, Button, Badge UI components.
- DaumPostcode modal: reuse the same overlay pattern from around-me page (fixed inset-0 z-[2000] backdrop).
- Image preview: use `URL.createObjectURL(file)` and revoke on cleanup.
- 5-state handling: loading during submit, error via toast (apiClient handles), success via toast + form reset.
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>EmergencyReportForm no longer imports geminiService or DOG_BREEDS. LOST mode submits via createLostPet with presigned URL upload. FOUND mode submits via createSighting. Both forms have image preview, DaumPostcode location, datetime picker.</done>
</task>

<task type="auto">
  <name>Task 2: Wire EMERGENCY tab in around-me page</name>
  <files>
    aini-inu-frontend/src/app/around-me/page.tsx,
    aini-inu-frontend/src/components/around-me/AroundMeHeader.tsx
  </files>
  <action>
**around-me/page.tsx:**
- Remove the "준비 중" overlay wrapping the EMERGENCY tab content (lines 179-197). Replace with EmergencySubTabs component.
- Import EmergencySubTabs. Render it when `activeTab === 'EMERGENCY'` in the same flex container.
- Remove the old EmergencyReportForm direct render and AICandidateList render (those are now inside EmergencySubTabs or will be in Plan 03).
- The map section (`RadarMapSection`) should NOT render when `activeTab === 'EMERGENCY'` -- the EMERGENCY tab is form-only (no map needed). Currently the condition is `(activeTab === 'FIND' || activeTab === 'EMERGENCY')` for map -- change to just `activeTab === 'FIND'`.
- Pass no special props to EmergencySubTabs for now (Plan 03 will add the analysis callback).

**AroundMeHeader.tsx:**
- Remove `opacity-50` class from the EMERGENCY tab button (line 111: `tab.id === 'EMERGENCY' && 'opacity-50'`). The tab should now look fully active/enabled.

**Cleanup:**
- Remove unused imports: `Siren` from page.tsx if no longer referenced, old `EmergencyReportForm` direct import if EmergencySubTabs handles it, `AICandidateList` import from page.tsx.
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>EMERGENCY tab renders sub-tabs without overlay. Map does not show for EMERGENCY tab. Tab button is no longer dimmed. Build passes with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes in aini-inu-frontend
- `npm run lint` passes
- No imports of `geminiService` or `DOG_BREEDS` remain in EmergencyReportForm
- EMERGENCY tab shows sub-tab navigation, not "준비 중" overlay
</verification>

<success_criteria>
- EMERGENCY tab is fully activated with sub-tabs
- Lost pet report form submits to backend via presigned upload + createLostPet
- Sighting form submits to backend via presigned upload + createSighting
- All geminiService dependencies removed from EmergencyReportForm
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-lost-pet/10-01-SUMMARY.md`
</output>
