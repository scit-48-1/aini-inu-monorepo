---
phase: 09-community-feed
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - aini-inu-frontend/src/components/profile/PostDetailModal.tsx
  - aini-inu-frontend/src/components/profile/ProfileFeed.tsx
  - aini-inu-frontend/src/components/profile/MyProfileView.tsx
  - aini-inu-frontend/src/components/profile/ProfileView.tsx
  - aini-inu-frontend/src/hooks/useMemberProfile.ts
  - aini-inu-frontend/src/services/api/postService.ts
autonomous: true
requirements: [FEED-03, FEED-04, FEED-05]

must_haves:
  truths:
    - "PostDetailModal displays post detail with PostResponse types and allows edit/delete via api/community.ts"
    - "ProfileFeed grid renders PostResponse with correct field names"
    - "MyProfileView and ProfileView no longer import FeedPostType or postService"
    - "postService.ts is deleted -- zero consumers remain"
  artifacts:
    - path: "aini-inu-frontend/src/components/profile/PostDetailModal.tsx"
      provides: "Post detail view/edit/delete modal using PostResponse + api/community.ts"
      contains: "updatePost"
    - path: "aini-inu-frontend/src/components/profile/ProfileFeed.tsx"
      provides: "Profile post grid using PostResponse"
      contains: "PostResponse"
  key_links:
    - from: "aini-inu-frontend/src/components/profile/PostDetailModal.tsx"
      to: "api/community.ts"
      via: "updatePost, deletePost imports"
      pattern: "import.*(updatePost|deletePost).*from.*api/community"
    - from: "aini-inu-frontend/src/components/profile/ProfileFeed.tsx"
      to: "api/community.ts"
      via: "PostResponse type import"
      pattern: "import.*PostResponse.*from.*api/community"
---

<objective>
Rewire profile-related post components to PostResponse types, wire PostDetailModal to api/community.ts for edit/delete, and delete the legacy postService.

Purpose: Complete the migration from legacy post types and services across all remaining consumer files, ensuring zero references to the old postService remain, allowing safe deletion.

Output: All profile post components use PostResponse. PostDetailModal handles edit/delete via API. postService.ts deleted.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-community-feed/09-CONTEXT.md
@.planning/phases/09-community-feed/09-RESEARCH.md
@.planning/phases/09-community-feed/09-01-SUMMARY.md
@.planning/phases/09-community-feed/09-02-SUMMARY.md

<interfaces>
<!-- Key types from api/community.ts (created in Phase 2, used in Plans 01+02) -->

From aini-inu-frontend/src/api/community.ts:
```typescript
export interface PostResponse {
  id: number;
  author: Author;
  content: string;
  imageUrls: string[];
  likeCount: number;
  commentCount: number;
  createdAt: string;
  liked: boolean;
}

export interface PostUpdateRequest {
  content: string;
  caption?: string;
  imageUrls?: string[];
}

export async function getPost(postId: number): Promise<PostDetailResponse>;
export async function updatePost(postId: number, data: PostUpdateRequest): Promise<PostResponse>;
export async function deletePost(postId: number): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire PostDetailModal and ProfileFeed to PostResponse types</name>
  <files>aini-inu-frontend/src/components/profile/PostDetailModal.tsx, aini-inu-frontend/src/components/profile/ProfileFeed.tsx</files>
  <action>
**PostDetailModal.tsx** -- Rewrite to self-contained API integration:
- Remove `import { FeedPostType, UserType } from '@/types'`.
- Import `PostResponse, updatePost, deletePost` from `@/api/community`.
- Import `useUserStore` from `@/store/useUserStore`.
- Simplify props: `{ isOpen: boolean; onClose: () => void; post: PostResponse | null; onUpdated?: (post: PostResponse) => void; onDeleted?: (postId: number) => void; }`.
- Remove external `user`, `isEditing`, `setIsEditing`, `editCaption`, `setEditCaption`, `onUpdate`, `onDelete` props -- manage edit state internally.
- Get current user from `useUserStore`: `const profile = useUserStore(s => s.profile)`.
- Show edit/delete buttons only when `post.author.id === Number(profile?.id)`.
- Field mapping in JSX:
  - `post.images?.[0]` -> `post.imageUrls?.[0]`
  - `post.caption` -> `post.content`
  - `post.likes` -> `post.likeCount`
  - `user.avatar` -> `post.author.profileImageUrl`
  - `user.nickname` -> `post.author.nickname`
  - `post.location` -> remove location display
- Internal edit state: `const [isEditing, setIsEditing] = useState(false)`, `const [editContent, setEditContent] = useState('')`.
- Edit button click: `setEditContent(post.content); setIsEditing(true)`.
- Save handler: validate `editContent.trim()` non-empty (content required per DEC-004), call `updatePost(post.id, { content: editContent })`, on success toast + call `onUpdated?.(updatedPost)` + `setIsEditing(false)`, on failure toast error.
- Delete handler: call `deletePost(post.id)`, on success toast + call `onDeleted?.(post.id)` + `onClose()`, on failure toast error.

**ProfileFeed.tsx** -- Type rewire:
- Remove `import { FeedPostType } from '@/types'`.
- Import `PostResponse` from `@/api/community`.
- Update props: `{ posts: PostResponse[]; onPostClick: (post: PostResponse) => void; }`.
- Field mapping in JSX:
  - `post.images[0]` -> `post.imageUrls[0]`
  - `post.likes` -> `post.likeCount`
  - `post.comments` -> `post.commentCount`
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit 2>&1 | grep -E "(PostDetailModal|ProfileFeed)" | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>PostDetailModal uses PostResponse, handles edit/delete internally via api/community.ts. ProfileFeed uses PostResponse with correct field names. No FeedPostType or UserType imports.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up profile views, useMemberProfile, and delete postService</name>
  <files>aini-inu-frontend/src/components/profile/MyProfileView.tsx, aini-inu-frontend/src/components/profile/ProfileView.tsx, aini-inu-frontend/src/hooks/useMemberProfile.ts, aini-inu-frontend/src/services/api/postService.ts</files>
  <action>
**MyProfileView.tsx:**
- Remove `import { FeedPostType } from '@/types'`.
- Change `const [posts, setPosts] = useState<FeedPostType[]>([])` to `useState<PostResponse[]>([])` with import from `@/api/community`.
- Change `const [selectedPost, setSelectedPost] = useState<FeedPostType | null>(null)` to `useState<PostResponse | null>(null)`.
- Update PostDetailModal usage: pass simplified props (`post={selectedPost}`, `onUpdated`, `onDeleted` callbacks that update posts array).
- Posts are currently never fetched in MyProfileView (removed in Phase 4). Keep the empty array for now -- the profile POSTS tab can show empty state. If ProfileFeed is rendered, it will show empty grid.

**ProfileView.tsx:**
- Remove `import { postService } from '@/services/api/postService'`.
- Remove `import { FeedPostType } from '@/types'` (keep DogType, UserType if still used by other parts).
- Remove `postService.getPosts(targetId)` from the Promise.all fetch block (backend GET /posts has no memberId filter).
- Change posts state to `PostResponse[]` from `@/api/community`.
- Change selectedPost state to `PostResponse | null`.
- Update PostDetailModal usage: same simplified props pattern as MyProfileView.
- Posts will remain empty array since there's no member-filtered post endpoint.

**useMemberProfile.ts:**
- Remove `import { postService } from '@/services/api/postService'`.
- Remove `import { FeedPostType } from '@/types'` (keep other type imports if needed).
- Remove `postService.getPosts(memberId)` from Promise.all.
- Change posts state to `PostResponse[]` with import from `@/api/community`.
- Remove posts from the destructured Promise.all result.

**Delete postService.ts:**
- After all consumers are migrated, delete `aini-inu-frontend/src/services/api/postService.ts`.
- Verify no remaining imports: search for `postService` across the codebase.
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit 2>&1 | head -30 && grep -r "postService" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | head -10; echo "Exit: $?"</automated>
  </verify>
  <done>MyProfileView, ProfileView, and useMemberProfile use PostResponse. No postService imports remain anywhere. postService.ts is deleted. `npm run build` passes.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npm run lint && npm run build
# Verify postService is fully gone
grep -r "postService" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules
# Should return empty
```
All profile components compile with PostResponse types. Zero references to postService remain. postService.ts deleted.
</verification>

<success_criteria>
- PostDetailModal handles view/edit/delete with api/community.ts (not parent callbacks)
- ProfileFeed uses PostResponse with imageUrls, likeCount, commentCount
- MyProfileView and ProfileView use PostResponse type for posts state
- useMemberProfile no longer fetches posts via postService
- postService.ts is deleted
- Zero grep matches for "postService" in src/
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-community-feed/09-03-SUMMARY.md`
</output>
