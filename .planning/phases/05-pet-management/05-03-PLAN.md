---
phase: 05-pet-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - aini-inu-frontend/src/components/profile/ProfileDogs.tsx
autonomous: true
gap_closure: true
requirements:
  - PET-01
  - PET-02
  - PET-03
  - PET-04
  - PET-05
  - PET-06
  - PET-07
  - PET-08

must_haves:
  truths:
    - "When zero pets exist, ProfileDogs renders a centered empty-state block with Dog icon, Korean message, and register button"
    - "The register button in empty state calls onAddClick to open the registration modal"
    - "When pets exist (length > 0), the normal card grid renders as before with no empty-state visible"
  artifacts:
    - path: "aini-inu-frontend/src/components/profile/ProfileDogs.tsx"
      provides: "Empty-state rendering when pets.length === 0"
      contains: "아직 등록된 반려견이 없습니다"
  key_links:
    - from: "ProfileDogs.tsx empty-state button"
      to: "onAddClick prop"
      via: "onClick handler"
      pattern: "onAddClick"
---

<objective>
Close the single verification gap from Phase 05: add an empty-state block to ProfileDogs when `pets.length === 0`.

Purpose: The verification report found that when a user has zero pets, ProfileDogs shows only the "Add New Partner" dashed button with no explanatory message. The 5-state coverage requirement specifies a dedicated empty state with a Dog icon, Korean text ("아직 등록된 반려견이 없습니다"), and a "반려견 등록하기" button calling `onAddClick`.

Output: Updated ProfileDogs.tsx with empty-state conditional rendering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@aini-inu-frontend/src/components/profile/ProfileDogs.tsx
</context>

<interfaces>
<!-- Current ProfileDogs interface from the codebase -->
From aini-inu-frontend/src/components/profile/ProfileDogs.tsx:
```typescript
interface ProfileDogsProps {
  pets: PetResponse[];
  onPetClick: (pet: PetResponse) => void;
  onAddClick: () => void;
}
```

Already imported: `Plus`, `Crown` from lucide-react; `Card`, `Typography`, `Badge` from UI components; `PetResponse` from `@/api/pets`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add empty-state block to ProfileDogs when pets.length === 0</name>
  <files>aini-inu-frontend/src/components/profile/ProfileDogs.tsx</files>
  <action>
Add `Dog` to the lucide-react import (alongside `Plus` and `Crown`).

In the component's return, add an early-return conditional: when `pets.length === 0`, render a centered empty-state block instead of the card grid. The empty state should contain:
1. A `Dog` icon (size 48, className `text-zinc-300 mb-4`)
2. A `Typography` element with text "아직 등록된 반려견이 없습니다" (variant `body`, className `text-zinc-400 mb-6`)
3. A button with text "반려견 등록하기" that calls `onAddClick` on click. Style the button with amber theme to match the existing add-button hover style: `bg-amber-500 text-white px-6 py-3 rounded-full font-bold hover:bg-amber-600 transition-colors`.

Wrap these three elements in a container div with `flex flex-col items-center justify-center p-12 min-h-[300px]` to center the content vertically and horizontally, consistent with the existing card grid padding.

Keep the existing card grid + add-button rendering for the `pets.length > 0` case completely unchanged.
  </action>
  <verify>
    <automated>cd /Users/keonhongkoo/Desktop/github/aini-inu/aini-inu-frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ProfileDogs renders "아직 등록된 반려견이 없습니다" with Dog icon and "반려견 등록하기" button when pets array is empty. When pets exist, the normal grid renders unchanged. Build passes with no type errors.</done>
</task>

</tasks>

<verification>
1. `grep -n "아직 등록된 반려견이 없습니다" aini-inu-frontend/src/components/profile/ProfileDogs.tsx` returns a match
2. `grep -n "반려견 등록하기" aini-inu-frontend/src/components/profile/ProfileDogs.tsx` returns a match
3. `grep -n "Dog" aini-inu-frontend/src/components/profile/ProfileDogs.tsx` shows Dog icon import from lucide-react
4. `npm run build` succeeds in aini-inu-frontend
</verification>

<success_criteria>
- ProfileDogs.tsx contains a conditional empty-state block for `pets.length === 0`
- Empty state shows Dog icon, Korean message, and register button wired to `onAddClick`
- Existing pet card grid and add-button behavior unchanged for non-empty pet lists
- Frontend build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-pet-management/05-03-SUMMARY.md`
</output>
